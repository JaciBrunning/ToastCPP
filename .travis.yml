language: cpp

matrix:
  include:
  - os: linux
    dist: trusty
    sudo: required
    compiler: gcc
  - os: linux
    dist: trusty
    sudo: required
    env: GRADLE_ARGS='-Parm -Proborio'
  - os: osx
    osx_image: xcode8

addons:
  apt:
    packages:
      - g++-multilib
      - lib32stdc++6

before_install:
- wpi/ntcore/.travis-scripts/install.sh

script:
- "./gradlew clean build $GRADLE_ARGS"

notifications:
  email: false